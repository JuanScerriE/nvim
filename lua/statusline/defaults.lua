local M = {
  mode = {
    disable = false,
    length = 80,
  },

  filename = {
    disable = false,
    length = 80,
  },

  filetype = {
    disable = false,
    length = 20,
  }

  lsp_status ={
    disable = true,
  },

  git_status = {
    disable = true,
  },
}

return M
